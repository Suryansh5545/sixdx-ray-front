# Backend server base URL used by frontend auth and token requests.
# Leave blank to use the same origin as the frontend.
VITE_SERVER_URL=

# LiveKit server URL used if /api/token does not return serverUrl.
VITE_LIVEKIT_SERVER_URL=
